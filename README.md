# Prisma Blog App

- After installing needed dependencies we have to create schema using prisma.
  migrate it and generate also.
- Now that we have database ready and others things we are going to do opn the go .
- Now creating a serverfile for running our app on local host for testing purpose
  lets do this by creating a file on src folder (you can name it as you want).
- Also for route another file would be there (naming is definitely your choice but professional)
- But at first following prisma doc instantiate prisma client for using prisma to create data.
